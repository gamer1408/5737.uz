import React from 'react';
import { useDualMode } from '@/hooks/useDualMode';
import { User, UserX, Database, HardDrive } from 'lucide-react';

export default function ModeIndicator() {
  // Component removed - no status display
  return null;
}